@model IEnumerable<JobSearchApp.Models.SavedSearch>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Candidate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SearchedTerm)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Candidate.UserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SearchedTerm)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SavedSearchID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SavedSearchID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SavedSearchID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
